name: Update Prices & Deploy

on:
  schedule:
    - cron: '0 */1 * * *' # Запускати щогодини
  workflow_dispatch: # Кнопка для ручного запуску

# Дозволи
permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build-and-deploy:
    # 👇 ОСЬ ЦЕЙ БЛОК МИ ДОДАЛИ, ЩОБ ВИПРАВИТИ ПОМИЛКУ
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    
    runs-on: ubuntu-latest
    
    steps:
    # 1. Скачуємо код
    - name: Checkout code
      uses: actions/checkout@v4

    # 2. Налаштовуємо Node.js
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 18

    # 3. Встановлюємо бібліотеки
    - name: Install Dependencies
      run: npm install

    # 4. Запускаємо твій скрипт
    - name: Run Scanner Script
      env:
        BLIZZARD_CLIENT_ID: ${{ secrets.BLIZZARD_CLIENT_ID }}
        BLIZZARD_CLIENT_SECRET: ${{ secrets.BLIZZARD_CLIENT_SECRET }}
      run: node index.js

    # 5. Зберігаємо історію цін
    - name: Commit and Push History
      run: |
        git config --global user.name "WoW Decor Bot"
        git config --global user.email "actions@github.com"
        
        git add price_history.json
        
        if git diff --staged --quiet; then
          echo "No changes in price history"
        else
          git commit -m "Update price history [skip ci]"
          git push
        fi

    # 6. Налаштування Pages
    - name: Setup Pages
      uses: actions/configure-pages@v4

    # 7. Завантажуємо папку public
    - name: Upload artifact
      uses: actions/upload-pages-artifact@v3
      with:
        path: 'public'

    # 8. Публікуємо сайт
    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4